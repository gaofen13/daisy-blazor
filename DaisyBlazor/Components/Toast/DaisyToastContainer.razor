@namespace DaisyBlazor
@inherits DaisyComponentBase

@if (ToastList.Any())
{
    <div id="@Id" class="@Classname" style="@StyleAttribute" @attributes="@Attributes">
        <CascadingValue Value="@this" IsFixed>
            @foreach (var toast in ToastList.OrderBy(x => x.TimeStamp))
            {
                <DaisyToast @key="toast" ToastSettings="toast.ToastSettings" ToastId="@toast.Id" ToastLevel="@toast.ToastLevel" Title="@toast.Title" ShowProgressBar="@GlobalSettings.ShowProgressBar">
                    @toast.MessageContent
                </DaisyToast>
            }
        </CascadingValue>
    </div>
}
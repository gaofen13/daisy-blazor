@namespace DaisyBlazor
@inherits DaisyComponentBase

@if (Visible)
{
    if (Mode == RenderMode.Body)
    {
        <td>@ChildContent</td>
    }
    else if (Mode == RenderMode.Head)
    {
        <th>@ChildContent</th>
    }
}

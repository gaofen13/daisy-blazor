<pre class="@Classname" style="padding:0.5rem 0;background-color: var(--fallback-n, oklch(var(--n) / var(--tw-bg-opacity)));"><code @ref="@_codeElement" class="@Language">
        @ChildContent
</code></pre>